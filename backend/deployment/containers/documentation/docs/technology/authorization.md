# Backend authorization model (enforced by API endpoints)

The Backend API documentation is visible under `/api/v1/docs/`.
It enforces authentication (login) on all relevant endpoints.
Authorization is enforced mainly on the endpoints tagged `user` in `backend/api_v1/user.py`.

Authorization rules are documented as part of the API documentation.
